#!/bin/bash

url=https://cheems.mirrorbomt.workers.dev/ccache-superior.tar.gz

cd /tmp # Where to download cccahe
time aria2c $url -x16 -s50
echo "dl done"
time tar xf ccache-superior.tar.gz
echo "unzip done"
find ccache-superior.tar.gz -delete
